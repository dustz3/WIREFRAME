@import '../variables'

.typography-sample
  display grid
  gap 32px

  .type-row
    display flex
    flex-direction column
    gap 8px

    .type-meta
      font-size 0.9rem
      color textLight
      letter-spacing 0.04em
      text-transform uppercase
